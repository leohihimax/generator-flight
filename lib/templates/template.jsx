define(['react', 'should be defined'], function(React, 'should be defined') {

    'use strict';

    var <%= _.classify(name) %> = {
        'should do something';
    }
    return <%= _.classify(name) %>;
});
define([], function () {

  'use strict';

  /**
   * Module exports
   */

  return <%= _.classify(name) %>;

  /**
   * Module function
   */

  function <%= _.classify(name) %>() {
    'should do something';
  }

});

define(['should be defined'], function('should be defined') {

    'use strict';

    function <%= _.classify(name) %>() {
        'should do something';
    }

    return defineComponent(<%= _.classify(name) %>);
});